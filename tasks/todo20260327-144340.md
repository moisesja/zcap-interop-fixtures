# Task Plan: Fixture Expansion And Issue-Ready Reporting

## Goal

Add missing isolation fixtures for known interoperability deficiencies and
extend the reporting flow so a run can produce findings grouped by repository in
an issue-ready format.

## Checklist

- [x] Add isolated fixtures for capability `type` preservation
- [x] Add paired fixtures for missing vs explicit empty `allowedAction`
- [x] Add paired fixtures for missing vs explicit empty `caveat`
- [x] Add invocation fixture for explicit empty `capabilityAction`
- [x] Add stronger numeric stress fixture(s) that remain neutral and portable
- [x] Extend the matrix runner to emit issue-ready findings grouped by target repository
- [x] Update sample config and docs for repository-targeted issue reporting
- [x] Verify the updated harness and generated artifacts

## Review

- Added 7 new fixtures focused on type preservation, missing-vs-empty field behavior, explicit empty capability action, and numeric JCS boundaries.
- Added issue-ready reporting that emits `ISSUES.md` and `issue-candidates.json` for repository-targeted follow-up when `issue_repo` is configured.
- Verified `python3 -m compileall python`.
- Verified `python3 python/run_matrix.py --config config/self-check.matrix.json`.
- Verified non-empty issue-candidate generation with a synthetic mismatch smoke test.
